# RPA SharePoint Connector
# Public-client OAuth (no client secret)

# Optional: override default app id shipped in code
MICROSOFT_CLIENT_ID=

# common | organizations | <tenant-guid>
MICROSOFT_TENANT_ID=common

# Optional: defaults to http://localhost/callback
MICROSOFT_BROWSER_REDIRECT_URI=http://localhost/callback
